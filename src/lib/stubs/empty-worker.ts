// Stub for worker: imports that Vite SSR can't resolve.
// These are optional PixiJS web worker loaders (compressed textures, image bitmap).
// In browser context they load fine; in SSR/build they need this no-op stub.
export default ''
