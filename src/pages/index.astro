---
/**
 * / — 重定向到 /generate (3-Step Pipeline)
 *
 * 旧的 4-step pipeline 已废弃 (commit ba6a4cd 确认)
 * 所有功能现在在 /generate 页面:
 *   Step 1: Claude Opus → Topology JSON → ELK Layout → Skeleton SVG
 *   Step 2: Grok 4 → 反推 prompt
 *   Step 3: Gemini 3 Pro Image → 科研级图片
 */
export const prerender = false
return Astro.redirect('/generate')
---