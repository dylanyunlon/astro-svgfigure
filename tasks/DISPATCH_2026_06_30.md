# 小弟任务分配 — 2026-06-30

管理者确认：M1308 修复了 /api/cells 的 dict→array bug，M1309 添加了 verify-render.html 验证工具。
tick-runner.py 已验证可用，58 个 cell 全部有 geometry.json 数据。

---

## 小弟 A：Artistoo 参考 + 验证闭环

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

完整指令：

1. git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
2. cd astro-svgfigure && git checkout cell-pubsub-loop
3. git clone https://github.com/ingewortel/artistoo.git upstream/artistoo
4. cd upstream/artistoo && tree -L 2 -I node_modules --dirsfirst
5. 读 upstream/artistoo/src/ 的结构，重点看 CPM.js 的 step() 方法和 Canvas.js 的 drawCells() 方法
6. 学习 Artistoo 的模式：config → new Simulation(config) → sim.step() → sim.drawCells() → requestAnimationFrame
7. 对比我们的 tick-runner.py + gpu-render-loop.ts，找出差距
8. 写一个 RESEARCH 文件 upstream/RESEARCH_131_artistoo.md，记录 Artistoo 的架构和我们能学到什么
9. 关键：npm install && bun run dev（如果 500 限流就多试几次），然后用浏览器打开 /verify-render.html 确认能看到 58 个彩色方块
10. 截图或记录控制台输出到 tasks/verify_render_result.md

别开新分支，直接 push 到 cell-pubsub-loop。
git config user.name "claude-worker-a" && git config user.email "worker-a@astro-svgfigure"
先 git pull origin cell-pubsub-loop 再改再 push。
```

---

## 小弟 B：bun dev 动态验证 + PBR 渲染调试

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

完整指令：

1. git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
2. cd astro-svgfigure && git checkout cell-pubsub-loop
3. npm install（如果某些包失败继续）
4. 先跑 tick-runner 生成最新数据：python3 channels/cell/tick-runner.py --ticks 5 --verbose
5. bun run dev（限流 500 多试几次，很正常）
6. 用 curl 测试 API：curl http://localhost:4321/api/cells | python3 -m json.tool | head -30
   确认返回的 cells 数组不为空，每个 cell 有 bbox
7. 如果 /api/cells 返回空或错误，查看 src/pages/api/cells.ts 的 readFromCompositeParams()
   M1308 已修了 dict→array bug，但如果还有问题就继续修
8. 打开 http://localhost:4321/verify-render.html 确认 58 个方块可见
9. 打开 http://localhost:4321/world 看 GPU 渲染。如果是黑屏或灰色方块：
   - 打开浏览器 DevTools Console，截取所有错误
   - 搜 "[PBRCellGPU]" "[SDFIconGPU]" "[GPURenderLoop]" 开头的日志
   - 记录到 tasks/gpu_render_debug.md
10. 如果 shader 编译失败，修 src/lib/sph/pbr-gpu-pass.ts 或 sdf-gpu-pass.ts

别开新分支，直接 push 到 cell-pubsub-loop。
git config user.name "claude-worker-b" && git config user.email "worker-b@astro-svgfigure"
先 git pull origin cell-pubsub-loop 再改再 push。
```

---

## 小弟 C：Unseen Studio 参考 + Kawase blur 后处理

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

完整指令：

1. git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
2. cd astro-svgfigure && git checkout cell-pubsub-loop
3. 搜索 Unseen Studio "When Cells Collide" 的技术细节：
   - 它用 Astro + Vite + Three.js + Rapier
   - 关键视觉效果是 Kawase blur 把简单圆环变成有机流体质感
   - 颜色映射到场景亮度（color grading by luminosity）
4. 我们的 upstream/pixijs-filters/ 和 upstream/pixijs-filters-v2/ 已经有 KawaseBlurFilter
   读 upstream/pixijs-filters/src/kawase-blur/ 的实现
5. 在 src/lib/sph/gpu-render-loop.ts 的 composite pass 之后，添加一个可选的 Kawase blur pass：
   - 不是用 PixiJS filter 系统（我们是原生 WebGL），而是手写一个简化版
   - 参考 pixijs-filters 的 kawase-blur shader（两个 pass：down + up）
   - 只对 cell 区域做 blur，保持边和文字清晰
6. 同时写 upstream/RESEARCH_132_unseen_cells.md 记录 Unseen Studio 的架构

别开新分支，直接 push 到 cell-pubsub-loop。
git config user.name "claude-worker-c" && git config user.email "worker-c@astro-svgfigure"
先 git pull origin cell-pubsub-loop 再改再 push。
```

---

## 小弟 D：动态 bun dev + SSE cell agent 架构

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

完整指令：

这是关键架构任务 — 让 cell agent 在 bun dev 运行时动态生成和更新。

1. git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
2. cd astro-svgfigure && git checkout cell-pubsub-loop
3. 目前 tick-runner.py 是手动跑的。我们需要它在 bun dev 时自动跑。
4. 修改 server.py 或创建新的 start_live_loop.py：
   - bun dev 启动时，同时启动 tick-runner 的后台进程
   - tick-runner 每 200ms 执行一个 tick
   - 每个 tick 更新 58 个 cell 的 geometry.json 和 status.json
   - 通过 SSE endpoint /api/cell-events 推送变化给前端
5. 前端 src/pages/world/index.astro 已经有 EventSource('/api/cell/live') 监听。
   确认 SSE 推送能被前端接收
6. 关键设计：
   - tick-runner 是唯一的物理引擎，每 tick 读 environment → 计算 → 写 geometry.json
   - 前端 GPURenderLoop 每帧读 geometry.json（通过 CellGeometryChannel）
   - SSE 只是通知"有更新"，不传输完整数据（数据通过文件系统）
7. 测试：bun dev 后，看 /world 页面的 cell 是否每 200ms 有位置/形状变化

别开新分支，直接 push 到 cell-pubsub-loop。
git config user.name "claude-worker-d" && git config user.email "worker-d@astro-svgfigure"
先 git pull origin cell-pubsub-loop 再改再 push。
```
