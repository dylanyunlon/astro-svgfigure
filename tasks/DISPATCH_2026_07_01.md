# 小弟任务分配 — 2026-07-01 · M1314

管理者诊断 (commit 012085de → M1313):

**已完成**: tick-runner 58个cell跑了10 ticks，geometry.json全部同步到public/。数据格式验证通过：每个cell有完整的metaball SDF (base_radius + lobes + surface PBR属性)、伪足、细胞膜、内部运动。composite_params.json 格式正确 (58 cells dict, 69 edge_routes dict, 58 species_assignment)。收敛状态 epoch 3 已收敛 max_delta=0.0。

**核心问题**: /world 页面仍然看不到正确的渲染输出。GPU管线审计报告 (tasks/gpu_pipeline_audit.md) 确认了根因:
1. 210个sph/*.ts文件，20+个try/catch静默吞错误 → 任何pass编译失败就变null被跳过
2. composite pass 吃掉null输入用1x1 placeholder纹理替代 → 结果近乎全黑
3. frame() 里 Nuke.render() 在 cell draw 之前覆盖了画面
4. PBR shader 独立验证 (verify-pbr.html 304行) 存在但没有接入geometry.json数据

**本轮目标**: 不扩展模块数量。把已有的数据 (geometry.json) 端到端打通到屏幕上。

---

## 小弟 A：最小闭环 — verify-pbr.html 接入真实 geometry.json

目标：验证 PBR SDF shader 能读取 geometry.json 并渲染出有机 metaball 形态。

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
cd astro-svgfigure && git checkout cell-pubsub-loop

任务：让 verify-pbr.html 从真实数据渲染

1. 读 public/verify-pbr.html (304行) — 目前 hardcode 了 cell uniform
2. 读 public/channels/cell/input/geometry.json — 真实 tick-runner 输出
3. 修改 verify-pbr.html:
   - fetch('/channels/cell/input/geometry.json') 拿真实数据
   - 用 geometry.json 的 sdf.base_radius, sdf.lobes[], surface.albedo, surface.roughness 等字段
   - 替换掉 hardcoded uniform
   - 关键: lobes 数组要传入 shader 作为 metaball union 的输入
4. 同时 fetch 3个不同 cell 的 geometry.json (input, self_attn, ffn)
   在同一个 canvas 上渲染3个 metaball cell — 验证多cell渲染
5. 如果 shader 编译失败, 记录 gl.getShaderInfoLog() 的完整错误
6. 成功后截取控制台输出到 tasks/verify_pbr_geometry_result.md

关键：
- 不要创建新文件名带 v2/v3 后缀
- 直接修改 verify-pbr.html (它就是为这个目的创建的)
- 目标是看到屏幕上有3个不同形态的有机 metaball，颜色来自 geometry.json

git config user.name "claude-worker-a" && git config user.email "worker-a@astro"
git pull origin cell-pubsub-loop && git add -A && git commit -m "M1314a: verify-pbr reads real geometry.json — 3 cells metaball SDF" && git push origin cell-pubsub-loop
```

---

## 小弟 B：修复 gpu-render-loop.ts 的 pass chain 断裂

目标：让 GPURenderLoop 的7-pass链条至少有 PBR + Composite 两个pass能工作。

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
cd astro-svgfigure && git checkout cell-pubsub-loop

任务：修复 pass chain 让 /world 页面能看到 cell

核心问题在 src/lib/sph/gpu-render-loop.ts (2349行)。

1. 读 tasks/gpu_pipeline_audit.md — 管理者已写好的诊断报告
2. 定位问题1: frame() 里的渲染顺序
   搜索 frame() 方法, 找到 PBR/Composite/Nuke 的调用顺序
   如果 Nuke.render() 在 PBR 之后覆盖了画面, 调整顺序或禁用 Nuke
3. 定位问题2: Composite pass 吃 null 纹理
   找到 composite-gpu-pass.ts 的 render() 方法
   如果输入纹理是 null, 不应该用 1x1 placeholder — 应该只画有效的pass输出
   最简方案: 如果只有 PBR 成功, composite 直接 blit PBR 的 FBO 到屏幕
4. 定位问题3: 20+ catch 静默吞错误
   不要全改 — 只确保 PBR (line 305, 已无try/catch) 和 Composite 能正常工作
   其他 pass (bloom, shadow, edge, msdf, glass, lumen) 失败可以跳过但必须在 HUD 显示
5. 测试方案:
   - 在 constructor 末尾加 console.info('[GPURenderLoop] Pass status:', {
       pbr: !!this.pbr, composite: !!this.composite, bloom: !!this.bloom, ...
     })
   - 在 frame() 里 PBR render 后加 console.info('[GPURenderLoop] PBR drawCalls:', ...)
6. 写修复记录到 tasks/pass_chain_fix.md

关键约束:
- 不要删除任何 pass 模块文件
- 不要新建文件
- 只改 gpu-render-loop.ts 和 composite-gpu-pass.ts
- 目标: /world 页面能看到58个彩色cell的轮廓, 即使没有bloom/shadow/edge也行

git config user.name "claude-worker-b" && git config user.email "worker-b@astro"
git pull origin cell-pubsub-loop && git add -A && git commit -m "M1314b: fix pass chain — PBR+Composite minimum viable render" && git push origin cell-pubsub-loop
```

---

## 小弟 C：geometry.json → CellData 桥接 — 让 GPURenderLoop 读 SDF 数据

目标：loadGPUScene() 目前只读 composite_params.json 的 bbox，没读 geometry.json 的 SDF 数据。打通这条路。

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
cd astro-svgfigure && git checkout cell-pubsub-loop

任务：让 loadGPUScene 读取 geometry.json 的 SDF 数据

1. 读 src/pages/world/index.astro 的 loadGPUScene() (line ~1727)
   目前它只从 composite_params.json 读 bbox+species → CellData
   CellData 接口 (gpu-render-loop.ts line ~55) 没有 SDF 字段

2. 读 src/lib/sph/cell-geometry-channel.ts — 这个模块已经能读 geometry.json
   但它是在 GPURenderLoop 内部用的, 没有在 loadGPUScene 里用

3. 修改方案:
   a. 扩展 CellData 接口, 加入 SDF 相关字段:
      sdfBaseRadius?: number;
      sdfLobes?: Array<{angle: number; distance: number; radius: number}>;
      sdfNoiseAmp?: number;
      sdfNoiseFreq?: number;
      surfaceGlowIntensity?: number;
      membraneThickness?: number;
      pseudopods?: Array<{target_cell: string; length: number; width: number; curl: number}>;
   
   b. 在 loadGPUScene() 里, 对每个 cell:
      const geoUrl = '/channels/cell/' + cellId + '/geometry.json';
      const geoRes = await fetch(geoUrl).catch(() => null);
      if (geoRes?.ok) {
        const geo = await geoRes.json();
        // merge geo 的 sdf/surface/membrane 字段到 CellData
      }
   
   c. 注意: 58个fetch要并发不要串行
      const geoPromises = cellIds.map(id => fetch(...).then(r => r.json()).catch(() => null));
      const geos = await Promise.all(geoPromises);

4. 验证: console.log 打出第一个 CellData 的完整内容, 确认 SDF 字段存在
5. 写记录到 tasks/geometry_bridge_result.md

关键:
- 不要改 cell-geometry-channel.ts — 它是 SSE 实时更新用的
- 只改 world/index.astro 的 loadGPUScene() 和 gpu-render-loop.ts 的 CellData 接口
- 这是初始加载路径, 不是实时更新路径

git config user.name "claude-worker-c" && git config user.email "worker-c@astro"
git pull origin cell-pubsub-loop && git add -A && git commit -m "M1314c: loadGPUScene reads geometry.json SDF data into CellData" && git push origin cell-pubsub-loop
```

---

## 小弟 D：PBR shader 接收 SDF 数据 — metaball lobes 渲染

目标：pbr-gpu-pass.ts 的 shader 目前只画 rounded-rect SDF。改为读 geometry.json 的 metaball lobes 数据。

```
你是 astro-svgfigure 项目的 Claude Code 小弟。

git clone https://<YOUR_GITHUB_TOKEN>@github.com/dylanyunlon/astro-svgfigure.git
cd astro-svgfigure && git checkout cell-pubsub-loop

任务：让 PBR shader 渲染 metaball SDF

1. 读 src/lib/sph/pbr-gpu-pass.ts — 找到 FRAG_SRC 和 VERT_SRC
   目前 SDF 函数可能只有 roundedRect 或简单 circle
   
2. 读 channels/cell/GEOMETRY_FORMAT.md — GPU shader 的 SDF 计算方法已经写在里面
   关键代码: cellSDF() 函数用 smooth union 把 base_radius + lobes 合并

3. 修改 pbr-gpu-pass.ts 的 FRAG_SRC:
   a. 加 uniform: u_baseRadius, u_lobeCount
   b. 加 uniform array: u_lobeAngle[8], u_lobeDistance[8], u_lobeRadius[8]
      (最多支持8个lobes, GEOMETRY_FORMAT.md里的典型值是1-4个)
   c. 替换 SDF 函数为 GEOMETRY_FORMAT.md 里的 cellSDF()
   d. 加 surface noise: noise(uv * u_noiseFreq) * u_noiseAmp
      noise 函数用简单的 2D value noise 或 simplex 就够
   
4. 修改 pbr-gpu-pass.ts 的 render() 方法:
   从 CellData 的新 SDF 字段读取 uniform 值
   如果 CellData 没有 SDF 字段 (小弟C还没push), 用 fallback 默认值:
   - base_radius = min(w, h) * 0.4
   - lobes = [] (0个)
   - noise_amplitude = 0.02
   
5. 验证: 用 verify-pbr.html 手动测试 shader 编译
   把修改后的 GLSL 复制到 verify-pbr.html 里测
   
6. 写记录到 tasks/pbr_sdf_upgrade.md

关键:
- 只改 pbr-gpu-pass.ts 一个文件
- shader 必须 #version 300 es 兼容
- lobe uniform array 固定大小 (MAX_LOBES=8), 用 u_lobeCount 控制实际数量
- smooth union k=0.3 (和 GEOMETRY_FORMAT.md 一致)
- 不要加新的 pass, 不要改 composite

git config user.name "claude-worker-d" && git config user.email "worker-d@astro"
git pull origin cell-pubsub-loop && git add -A && git commit -m "M1314d: PBR shader metaball SDF — lobes + smooth union + surface noise" && git push origin cell-pubsub-loop
```

---

## 执行顺序与依赖关系

```
小弟 A (verify-pbr 验证)  ────→  独立, 无依赖, 第一个跑
小弟 D (PBR shader 改造)  ────→  独立, 无依赖, 可以和A同时跑
小弟 C (loadGPUScene桥接) ────→  独立, 无依赖, 可以同时跑
小弟 B (pass chain 修复)  ────→  最好在 C/D 之后, 但也可以同时跑
                                   (B改的是调用逻辑, C/D改的是数据/shader)

理想合并顺序: A → D → C → B
A 验证 shader 能工作 → D 把 shader 改成 metaball → C 把数据接入 → B 修通 pass chain
```

## claude-hk-config 同步

当前 claude-hk-config 状态应更新为:

```json
{
  "project": "astro-svgfigure",
  "branch": "cell-pubsub-loop",
  "latest_commit": "012085de",
  "milestone": "M1314",
  "epoch": 3,
  "convergence": "converged",
  "cells_total": 58,
  "cells_with_geometry": 58,
  "ticks_completed": 16,
  "render_status": "data_ready_pipeline_broken",
  "blocking_issue": "gpu_pass_chain_disconnected",
  "pass_status": {
    "PBR": "init_ok_but_no_sdf_data",
    "Composite": "eating_null_inputs",
    "Bloom": "catch_swallowed",
    "Shadow": "catch_swallowed",
    "Edge": "catch_swallowed",
    "MSDF": "catch_swallowed",
    "Particle": "catch_swallowed"
  },
  "data_validation": {
    "composite_params": "58_cells_69_edges_ok",
    "geometry_json": "58_cells_all_have_sdf_lobes_surface",
    "species_assignment": "58_cells_mapped"
  },
  "worker_dispatch": "M1314_4_workers",
  "worker_tasks": [
    "A: verify-pbr reads real geometry.json",
    "B: fix pass chain PBR+Composite",
    "C: loadGPUScene bridge geometry.json → CellData",
    "D: PBR shader metaball lobes + smooth union"
  ],
  "next_milestone": "M1315_first_organic_render_on_world_page"
}
```
