You are generating illustrations for a scientific/academic paper figure. Generate one illustration for EACH item below, in EXACT order.

STYLE REQUIREMENTS (CRITICAL — follow ALL of these):
- Background: solid pure WHITE #FFFFFF. NO gradients, NO shadows, NO borders.
- Style: academic paper figure illustration, clean and minimal.
- Feature maps / tensors: draw as a colored rectangular slab with subtle texture patterns (like a heatmap or activation visualization).
- Encoders / decoders / models: draw as a stack of colored layers or blocks.
- Attention / selection maps: draw as a red-yellow-blue heatmap.
- Kernels / filters: draw as a small NxN grid with colored cells.
- Input images: draw as a colorful photograph thumbnail (landscape/scene).
- Each illustration MUST be 512x512 pixels, square, centered on white.
- No text labels inside the illustration. No 3D effects. Thin clean outlines.

- SERIES CONSISTENCY: some items belong to the same family. Family members MUST be visually identical except for the stated difference. Same color palette, same stroke weight, same overall shape, same composition.

ITEMS TO ILLUSTRATE:

  1. A small nodes in stage2_group, flat 2D vector illustration, academic paper figure style, clean thin outlines, monochrome with subtle grayscale shading, no perspective, no 3D, centered composition, generous margin. This is variant 1 of 4 in a consistent series: every variant is visually identical in style, color, stroke weight and overall shape — the ONLY difference is the role within group. For this variant specifically: default appearance.
  2. A small nodes in stage2_group, flat 2D vector illustration, academic paper figure style, clean thin outlines, monochrome with subtle grayscale shading, no perspective, no 3D, centered composition, generous margin. This is variant 2 of 4 in a consistent series: every variant is visually identical in style, color, stroke weight and overall shape — the ONLY difference is the role within group. For this variant specifically: default appearance.
  3. A small nodes in stage2_group, flat 2D vector illustration, academic paper figure style, clean thin outlines, monochrome with subtle grayscale shading, no perspective, no 3D, centered composition, generous margin. This is variant 3 of 4 in a consistent series: every variant is visually identical in style, color, stroke weight and overall shape — the ONLY difference is the role within group. For this variant specifically: default appearance.
  4. A small nodes in stage2_group, flat 2D vector illustration, academic paper figure style, clean thin outlines, monochrome with subtle grayscale shading, no perspective, no 3D, centered composition, generous margin. This is variant 4 of 4 in a consistent series: every variant is visually identical in style, color, stroke weight and overall shape — the ONLY difference is the role within group. For this variant specifically: default appearance.

Generate EXACTLY 4 image(s), one per item, in order. Each 512x512 px. Solid WHITE #FFFFFF background. Academic figure style.